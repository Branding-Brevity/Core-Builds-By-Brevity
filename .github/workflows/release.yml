name: Package Release Assets

on:
  release:
    types: [published]

jobs:
  package-and-upload:
    name: package-and-upload
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Collect template + formatter assets
        run: |
          mkdir -p release_assets
          find Templates/ -name "*.json" ! -path "*/Deprecated/*" -exec cp {} release_assets/ \;
          find Formatters/ -name "*.json" -exec cp {} release_assets/ \;
          echo "Assets collected:"
          ls release_assets/

      - name: Zip all assets
        run: |
          cd release_assets
          zip -r ../Core-Builds-${{ github.event.release.tag_name }}.zip .
          cd ..

      - name: Upload assets to release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release_assets/*.json
            Core-Builds-${{ github.event.release.tag_name }}.zip
          fail_on_unmatched_files: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
